schemaVersion: 1
sessionDefaults:
  projectPath: ./ios/MimiRemote/MimiRemote.xcodeproj
  scheme: MimiRemote
  configuration: Debug
  # 不持久化任何 device/simulator 目标。日常 build/run 统一走 ios-dev.sh；
  # 测试和快照按脚本解析结果在当前 session 同时设置 simulatorId 与 DerivedData。
  bundleId: com.gaixianggeng.mimi
